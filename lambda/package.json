{
  "name": "email-processor-lambda",
  "version": "1.0.0",
  "type": "module",
  "description": "AWS Lambda function to process inbound emails from SES and forward to app webhook",
  "main": "index.mjs",
  "scripts": {
    "package": "npm install && zip -r function.zip index.mjs node_modules"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "mailparser": "^3.7.1"
  }
}
